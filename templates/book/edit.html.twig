{% extends 'base.html.twig' %}

{% form_theme book_form 'bootstrap_5_layout.html.twig' %}

{% block title %}
    {{ parent() }} :: edit Book
{% endblock %}

{% block body %}
    <h2>Editer un livre</h2>
    <div class="flex justify-content">
        {{ form_start(book_form, {'attr' : {'class' : 'une-classe' }}) }}

        {{ form_widget(book_form) }}


        {{ form_end(book_form) }}
    </div>
{% endblock %}